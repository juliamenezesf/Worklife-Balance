export default function Contact() {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-2">Contato</h1>
      <p className="text-sm text-gray-600 dark:text-gray-300">
        Formulário simples para contato (não precisa integrar com a API).
      </p>
    </section>
  );
}
